---
import BaseLayout from '../../layouts/BaseLayout.astro';
import HomeBody from '../../components/pages/HomeBody.astro';
import { t } from '../../content';

const locale = 'en' as const;
const meta = t(locale).home.meta;
---

<BaseLayout locale={locale} title={meta.title} description={meta.description} cta={false} closingTone="surface-2">
  <HomeBody locale={locale} />
</BaseLayout>
